export type Product = {
  id?: number;
  title: string;
  price: number;
  desc?: string;
  imgUrl?: string;
}

export interface ProductType {
  id?: number;
  title: string;
  price: number;
  desc?: string;
  imgUrl?: string;
}
